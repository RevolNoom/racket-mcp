# Work Queue 004: `racket-mcp` — Stage S4 Protocol engine (L2)

> **Source vision:** `docs/aide/vision.md`
> **Source roadmap:** `docs/aide/roadmap.md` (Stage S4, lines 164–195)
> **Source progress:** `docs/aide/progress.md` (Stage S4 section)
> **Reference impl:** MCP TypeScript SDK v2 (`typescript-sdk/`) — authoritative for any wire/correlation ambiguity. Source file: `packages/core/src/shared/protocol.ts` (M11 engine). Test reference: `packages/core/test/shared/protocol.test.ts` (correlation, timeout, cancellation, progress, concurrency, server-initiated requests, malformed-input handling).
> **Stage focus:** Stage S4 **only** — M11 (Protocol engine), `mcp/core/shared/protocol.rkt`. Stages S1 (items 001–009 ✅), S2 (items 010–018 ✅), and S3 (items 019–023, queue-003) are **not** revisited. Stage S5 (roles M12/M13) and all later modules are **deferred to queue-005 and later** — this batch does **not** spill into role code.
> **Queue number:** 004 (fourth queue; items numbered sequentially from **024**, continuing after queue-003's reserved items 019–023).
> **Sizing:** 6 items, each a genuine S4-sized (sub-multi-day) unit, all testable locally via `raco test`. S4 is a single **substantial, concurrency-heavy** module split into testable slices (scheduler/outbound → dispatch/context → negotiation → ported tests → portability sweep → demo+closeout), mirroring how queue-003 sliced S3. Total batch ≈ one week.

---

## Why this batch

Stage S3 (queue-003, items 019–023) opened layer L1: the hexagonal **transport port** (M6) and the **in-memory paired adapter** (M10). **Stage S4 opens layer L2** with the single shared engine — **M11, the protocol engine** (`mcp/core/shared/protocol.rkt`, mirroring TS `shared/protocol.ts`) — that **both roles compose** in S5. The engine owns the hard concurrency: outbound `request`/`notification` with id assignment and a response-resolver registry, an id-keyed in-flight scheduler over threads/channels/`sync`, per-request custodian + `cancel-evt`, timeouts, progress correlation, cancellation, inbound dispatch to method-keyed handlers, a per-inbound-request **handler context** (mirrors TS `RequestHandlerExtra`), and capability + protocol-version **negotiation**. It binds the M6 port and is tested over the M10 in-memory pair — **no real I/O** in this stage.

The stage splits into sub-multi-day, independently testable units: the **scheduler + outbound interface** (id assignment, in-flight registry, timeout, progress, cancellation) is the substantive concurrency engineering and ships its own correlation/timeout/cancel tests inline; **inbound dispatch + handler-registration + handler-context** is its own item because the `RequestHandlerExtra`-equivalent surface (cancel signal, `send-notification`/`send-request`, request-id, session/HTTP info) is the seam future stages extend and must be settled with its own tests; **capability + protocol-version negotiation** (`assert-capability-for-method` guards, one-shot negotiation at `initialize`, `UnsupportedProtocolVersion`) is its own item; the **ported `protocol.test.ts` subset** (concurrency / no-HOL-blocking / server-initiated / malformed-input → JSON-RPC error) is its own item per house style; and the **portability sweep + parity touch** and **demo + closeout** each earn an item.

**Dependency gate — S3 (queue-003) MUST land first.** S4 depends on **S1** (types M1 + errors M2), **S2** (utils, schema, validators), and **S3** (it **binds the M6 transport port** and runs every test over the **M10 in-memory pair**). Do **not** begin S4 work until items 019–023 are ✅; the engine cannot be wired or tested without the M6 port shape and a working in-memory adapter.

**Forward seams — present-but-inert in S4 (flag, do not "remove").** (1) The handler-context is the structure into which **S8 server auth later injects `AuthInfo`** (F7) — define the context with room for it; do not strip the field hook. (2) `send-request` in the handler-context is the path **S7b server-initiated requests** flow over, and is what sets `related-request-id` on the transport `send` once **M8** makes that option load-bearing — in S4 (in-memory) it is exercised structurally but `related-request-id` remains inert (single channel). (3) Protocol-version negotiation **gates the N1 façade** in later stages. These are wired now and consumed later — flagged so the Worker does not mistake them for dead weight.

**Composition invariant (architecture §4.1, S1) is an explicit S4 acceptance criterion.** The engine is a **standalone composable unit — no role subclassing**. Every S4 test must construct the engine **directly** (not via a `Server`/`Client`); a role that "is-an" engine is a defect, not a shortcut.

**Concurrency NFR is load-bearing, not incidental.** Concurrent in-flight requests must resolve **independently with no head-of-line blocking**; a slow or never-answered request must not stall others. This is asserted directly in the ported test suite, not assumed.

**Prerequisite for the Worker:** the TS reference checkout at `typescript-sdk/` must be available — `shared/protocol.ts` is authoritative for the request/response correlation, progress, cancellation, and negotiation surface, and `test/shared/protocol.test.ts` for the cases to port. Every S4 deliverable imports **only** L0 (S1 types/errors, S2 utils) + L1 (M6 port, M10 in-memory for tests); it must **not** pull in any role (`server`/`client`), subprocess, socket, or `web-server` module.

---

### Item 024: Protocol engine — scheduler + outbound `request`/`notification` (M11 core)
Implement the core of `mcp/core/shared/protocol.rkt` — the outbound interface and the internal in-flight scheduler, mirroring TS `shared/protocol.ts`. **Outbound `request`:** assign a unique request id, register a **response resolver** in an **id-keyed in-flight registry**, transmit via the bound M6 transport `send`, apply a **per-request timeout** (rejecting with the correct SDK error on expiry and reaping the in-flight entry), accept an optional **progress callback** correlated to the originating request, and support **cancellation**. **Outbound `notification`:** fire-and-forget, no id, no resolver. **Internal scheduler:** id-keyed in-flight registry; correlation of inbound responses (by id) back to the waiting resolver; thread/channel/`sync`-based delivery; **per-request custodian + `cancel-evt`** so cancellation tears down exactly the one request and reaps its entry. Engine binds the **M6 port** (item 019) and accepts a transport at construction; imports L0 (S1 types/errors, S2 utils) + the M6 port only — **no role code**. **Ships its own core test** (`mcp/core/shared/test/protocol-test.rkt`, extended by item 027) per house style. Testable: under `mcp/core/shared/test/`, the core suite constructs the engine **directly** over an **M10 in-memory pair** and asserts: a `request` correlates its response back to the caller by id; a request that **times out** rejects with the correct SDK error and its in-flight entry is **reaped**; **cancellation** propagates via `cancel-evt`/custodian and reaps the entry; a `notification` sends with no id and no resolver. `raco test` passes.

### Item 025: Inbound dispatch + handler-registration + handler-context (`RequestHandlerExtra`)
Add the inbound half of `mcp/core/shared/protocol.rkt`: dispatch inbound messages by **method** to **registered handlers**, plus the per-inbound-request **handler-context**. **Handler-registration interface:** register **request handlers** and **notification handlers** keyed by method (replace/clear supported); an inbound request with no registered handler yields a **method-not-found JSON-RPC error** (engine keeps running). **Inbound response routing:** inbound responses/errors route to the item-024 in-flight resolver (by id); **`notifications/progress`** routes to the originating request's progress callback. **Handler-context** (mirrors TS `RequestHandlerExtra`): per-inbound-request value carrying a **cancellation signal** (fires if the peer cancels that request), **`send-notification`** and **`send-request`** bound to this engine (the latter for **server-initiated requests**; the path that sets `related-request-id` on transport `send` once M8 lands — inert in S4), the **request-id**, **session info**, and **HTTP transport info** (absent for in-memory/stdio). **Leave the context extensible for the S8 `AuthInfo` injection** (F7) — do not strip the hook (see batch note). Imports item-024 engine internals + L0 + M6 port; **no role code**. Testable: extend `protocol-test.rkt` — over an in-memory pair, a registered request handler is invoked and its reply correlates back to the caller; a notification handler fires; an unregistered method yields a method-not-found JSON-RPC error with the engine still live; the handler-context exposes cancel-signal / `send-notification` / `send-request` / request-id / session / http-info, and a **server-initiated `send-request`** from within a handler round-trips back to the peer. `raco test` passes.

### Item 026: Capability + protocol-version negotiation (`assert-capability-for-method`, `UnsupportedProtocolVersion`)
Add the capability/version interface to `mcp/core/shared/protocol.rkt`, mirroring TS `shared/protocol.ts`. **`assert-capability-for-method`-style guards:** before issuing or dispatching a method, assert the relevant peer/self capability is present; an **out-of-capability method is rejected** with the correct SDK error (and inbound, a JSON-RPC error — no crash). **Protocol-version negotiation:** negotiate against **`SUPPORTED_PROTOCOL_VERSIONS`** (from S1/S2), surfacing **`UnsupportedProtocolVersion`** for an unknown/unsupported version; negotiation happens **once at `initialize`** and the negotiated version is recorded on the engine to **gate later interface use** (the N1 façade in future stages). The engine provides the negotiation **machinery**; the actual `initialize` *handler* is a role concern deferred to S5 — keep this item to the reusable guard/negotiation surface, **no role code**. Imports items 024–025 + L0 + M6 port. Testable: extend `protocol-test.rkt` — an out-of-capability method is **rejected** with the correct SDK error (outbound) / JSON-RPC error (inbound) and the engine stays live; version negotiation **accepts a supported version** and records it; an **unsupported version surfaces `UnsupportedProtocolVersion`**; negotiation is **one-shot** (re-negotiation is rejected or no-ops per TS semantics). `raco test` passes.

### Item 027: Ported `protocol.test.ts` subset — concurrency, progress, server-initiated, malformed-input
Port the substantive subset of TS `packages/core/test/shared/protocol.test.ts` into `mcp/core/shared/test/protocol-test.rkt`, building on the core cases from items 024–026. **Cases to land:** **request/response correlation** under load; **timeout** rejection; **cancellation** propagation + in-flight reaping; **progress** callback fires for `notifications/progress` correlated to the originating request; **N concurrent in-flight requests resolve independently with NO head-of-line blocking** (a slow/never-answered request must not stall the others — Concurrency NFR, asserted directly, e.g. a later-issued request resolves while an earlier one is still pending); **server-initiated requests** (engine-to-engine request originated from within an inbound handler, round-tripping over the in-memory pair); **malformed inbound message → correct JSON-RPC error response with the engine still running** (Reliability NFR — induce a bad/undispatchable payload, assert error reply + continued liveness). All cases construct the engine **directly** over an **M10 in-memory pair** (composition invariant). Imports the engine (items 024–026) + the M10 in-memory adapter (item 020) + L0. Testable: `raco test` over `mcp/core/shared/protocol.rkt` and the full `protocol-test.rkt` suite passes, exercising every listed case; the no-HOL-blocking and malformed-input-no-crash assertions are present and green.

### Item 028: S4 portability sweep + composition-invariant check + parity-matrix touch
Add the **S4 restricted-load portability check**, lock in the composition invariant, and update the parity matrix. **(1) Restricted-namespace load test** (under `mcp/core/shared/test/`) asserting that requiring `mcp/core/shared/protocol.rkt` pulls in **no subprocess module, no socket/TCP module, and no `web-server` module** (Portability NFR) — the engine is pure threads/channels/`sync` + the M6 port, with no transport-specific I/O. **(2) Composition-invariant assertion** — a test (or assertion within the load test) confirming the engine is a **standalone composable unit constructed directly, with no role subclassing / no `server`/`client` import** in its module graph (architecture §4.1). **(3) Parity-matrix touch** — update the roadmap §9 / `docs/aide/progress.md` parity-matrix row for **`protocol.ts` (M11) to `partial`** (module exists; full conformance exercise deferred to S9). Imports the engine + L1 only. Testable: `raco test` over all of `mcp/core/shared/` is green; the restricted-load test passes (no subprocess/socket/web-server, no role module pulled in); the parity-matrix/progress edit for `protocol.ts` is present and set to `partial`.

### Item 029: Stage S4 demo + closeout
Add the S4 demo script (e.g. `mcp/core/shared/demo/s4-demo.rkt`) — a **two-engine in-memory harness** exercising the stage end-to-end: construct **two engines** wired through an **M10 in-memory pair**; engine A issues a `request`, engine B **dispatches to a registered handler and replies**; then demonstrate a **concurrent second request resolving out of order** (no head-of-line blocking), a **timeout** (a request to a deliberately-unanswered method rejecting with the SDK error), and a **cancellation** (a pending request cancelled via `cancel-evt`, its in-flight entry reaped) — printing each outcome in order so async correlation is visibly demonstrated. Update `docs/aide/progress.md` to flip the Stage S4 acceptance boxes and mark Stage S4 status (📋 → ✅ as appropriate), and confirm the parity-matrix row touched in item 028 (`protocol.ts`) is recorded. Testable: the demo script runs end-to-end via `racket`; `raco test` is green across all of `mcp/core/shared/`; the progress edits are present. (Completes Stage S4; unblocks queue-005 / Stage S5 — MVP roles M12a low-level `Server` + M13 `Client`, which compose this engine to complete `initialize` + `tools/list` + `tools/call` over the in-memory transport.)
